# 为什么这个 idea 的数据集特别难构建

> 写作日期：2026-08-31
> 对象：`idea/01_idea.md`（SemTrace）的数据可行性诊断
> 前置材料：`dataAnalyze/R01_REPORT.md`（真实数据实证）、`idea/TGDataset分析.md`、`idea/04_RECOMMENDED_RICH_DATASET_BLUESKY.md` §5
>
> 本文回答一个问题：为什么找了这么多数据集，没有一个能直接用？
> 结论是——**这不是运气问题，是机制层面的必然。**

---

## 一、核心矛盾：两个要素在机制上互斥

`01_idea.md` 的任务观测量是两样东西：

1. **关系网络 + 感染分布**（网络侧证据）
2. **节点内容状态之间的复用与演化关系**（内容侧证据）

要让这个任务有意义，两样必须**同时存在于同一个传播事件中**。但真实传播行为决定了它们互斥：

| 传播方式 | 平台记录边？ | 内容变化？ |
|---|---|---|
| 点转发按钮（repost / retweet / forward） | ✅ 有边 | ❌ 逐字复用，零演化 |
| 手动复制 / 改写 / 截图 / 翻译 / 去归因 | ❌ 无边 | ✅ 有演化 |

**有边的地方没有演化，有演化的地方没有边。**

这两件事是**同一个动作的两面**：
- 用按钮转发 → 平台自动生成关系边，但内容被原样复制，用户没有机会编辑；
- 要编辑内容 → 必须手动复制粘贴 / 截图 / 转述，而这个动作**平台不会记录任何关系边**。

因此，`01_idea.md` 需要的「**关系图完整 + 内容演化密集**」这一组合，**在任何单一真实数据集上都不可能同时高**。

> 这不是数据集作者偷懒，也不是采集不够努力，**是传播行为本身的结构**。

### 1.1 所有已考察数据集都符合这条规律

| 数据集 | 关系图 | 内容演化 | 失败原因 |
|---|---|---|---|
| **MILD-Weibo** | ✅ 真实 follower 图（294,577 边） | ❌ 编辑对中位数 **0.5**，110 个事件中 55 个为零 | 全是按钮转发 |
| **CHECKED-Fake** | ❌ 无图（共现代理图 T=2 仅 115 条边，185/211 事件子图为空） | ❌ 主要是评论 | 两样都缺 |
| **MemeTracker** | ⚠️ 仅站点超链接图，非传播图 | ✅ 现成 phrase cluster，变体密集 | 有演化没传播图 |
| **TGDataset** | ✅ `forwarded_from_id` 可聚合频道图 | ✅ `is_forwarded=false` 段落有演化 | **两类此消彼长**：`is_forwarded` 高则复用多演化少，低则反之；且 71GB 分片问题（见 `TGDataset分析.md` §5） |
| Bluesky v3 / Sheng-Weibo / Weibo-COV | ✅ | ❌ | root-post 采样，结构性缺变体 |

**一以贯之：不是找得不够，是这个组合在真实世界里稀缺。**

### 1.2 更深一层：采样单位的问题

除了上述机制互斥，还有第二层障碍：**主流传播数据集以 repost 树为采样单位**。

一个事件 = 一个原帖 + 它的转发树。这种采样在设计上就保证了「一个 claim 只出现一个版本谱系」，
**天然排除了「同一 claim 的多个独立编辑版本共存」**——而后者正是 `01_idea.md` 的内容演化所需要的。

实测（跨事件 root 帖两两比对，字符 bigram Jaccard）：

| 数据集 | root 帖数 | 总配对 | jac≥0.9（近重复） | 0.6–0.9（编辑关系） |
|---|---|---|---|---|
| MILD-Weibo | 509 | 129,286 | 22 | **17** |
| CHECKED-Fake | 211 | 22,155 | 5 | **33** |

两个数据集加起来只有 50 对编辑关系。**变体不是不存在于微博，是被采样方式过滤掉了。**

> 因此数据选择的正确判据是「**采样单位是不是 claim/内容簇**」，而不是「平台数是几个」或「规模有多大」。

---

## 二、但这恰恰是这个 idea 成立的理由

把 §1 的表再读一遍，它其实就是 `01_idea.md` Introduction 那句话的**机制解释**：

> "真实社交内容传播并不总能由平台记录的 repost、retweet、quote 或 reply 等显式交互完整刻画"

**显式关系为什么不完整？因为内容一旦被编辑，它就是手动搬运的，而手动搬运不留边。**

所以问题设定不是凭空假设的，它被这个机制**保证**为真。换个角度看：

> **如果这样的数据集现成存在，这个问题早就被人做了。
> 数据的稀缺性本身，就是这个 idea 还没被占坑的原因。**

---

## 三、三条可选方向

真正的决策不是「找哪个数据集」，而是「**接不接受数据即贡献**」。

### 方向 A-：只做内容溯源

在内容相关的传播数据集中做idea的验证在进行

不要关系网络了，只做内容溯源。但是不确定目前的内容溯源有没有人做过内容复用和内容演化这个idea

担心： 你的组合点是：用"内容变体之间的编辑不对称性"作为方向证据，在候选源假设下联合关系结构做全局源点定位。据我所知这个具体组
  合没有现成工作，但我没法保证——需要真正查文献。

  必须查的关键词（你或我在通网环境下做）：
  "source localization" + "text reuse"
  "provenance" + "information cascade" + "source detection"
  "rumor source detection" + "content mutation" / "text variants"
  "meme evolution" + "directionality" / "phylogeny"
  "textual phylogeny" / "text phylogeny tree"          ← 这个最危险
  "multimedia phylogeny" (Dias, Rocha et al.)          ← 这个也很危险

  特别提醒最后两个：textual phylogeny 和 multimedia phylogeny
  是一整支已有研究，做的就是"从一组变体重建演化树/找根节点"，用的正是"编辑操作不对称"这个思想。这可能和你的 idea
  高度重叠。 在投入任何工程之前，这一支必须先查清楚。如果他们已经做了"从变体找根"，你的增量就只剩"联合关系图" ——
  那论文的立论要重新设计。 ：出现idea冲突

### 方向 A：数据即贡献（推荐）

放弃「找到一个完美数据集」，改为**分工**——三个来源各承担一段，没有一个需要同时满足所有条件：

| 角色 | 用什么 | 解决什么 |
|---|---|---|
| **训练 / 规模** | 真实级联（Bluesky v3 / Sheng-Weibo / Wb-MSF）+ LLM 施加编辑算子的**半合成**数据 | 样本量无上限，解决 MILD-Weibo 仅 110 事件、仅 4 个 root 用户的问题；**编辑不对称强度可作为实验变量**，能画出「方向信号强度 vs 定位精度」曲线——这是纯真实数据永远画不出的图 |
| **内容侧真实评测** | MemeTracker（约 250MB，`clust-*` 自带 phrase cluster，无抽样稀释问题） | 证明演化方向性在真实内容数据上成立，且跨生态成立 |
| **关系侧真实评测** | MILD-Weibo（唯一具备真实 follower 图） | R01 的复用/演化对照结果可直接进论文 |

- **合法性**：半合成在传播溯源领域是标准做法（现有 SL 工作大量使用 SI/SIR 仿真级联），审稿人接受度高。
- **代价**：1–2 个月工程。
- **收益**：论文多一条 dataset contribution；贡献更硬，且别人难以复制。
- **TGDataset 降为可选**：性质最好（`is_forwarded` 自带复用/演化标注、载荷完整），但代价（71GB 不可削减 + 服务器带宽未知 + claim 聚类工程）与收益不匹配，**不应卡在关键路径上**。

### 方向 B：缩小 idea 去适配现有数据

把「语义级编辑」缩小为「**结构性演化**」（`//@` 归因链的单调累积、平台字数截断），
则 MILD-Weibo 立刻够用，R01 已验证信号存在（58.32%）。

- **代价**：论断显著变弱；且依赖微博平台特有机制，**跨平台泛化性会被审稿人直接攻击**
  （Bluesky / Telegram 无 `//@` 机制，见 R01 §8.1——这已被列为当前结论最大的外部效度威胁）。
- **适用**：时间紧、只求一篇能投的稿件时的退路。

### 方向 C：换机制

如果连 MemeTracker 这种「内容演化密度最高、且设计目标就是追踪变体」的数据上都验证不出方向性，
则说明「**编辑操作的不完全对称性携带传播方向**」这个假设本身有问题。

- 此时应当**回头修改机制假设**，而不是继续找数据。
- 这也是为什么下一步必须先做 MemeTracker 验证——它是 A 与 B 的共同前提。

---


目前要做的第一步是使用内容溯源相关数据集，去验证我们的idea, 是不是存在内容复用和内容演化这种情况，Quotebank、MemeTracker、Viral Texts